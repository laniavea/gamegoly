import { FieldMainData, DiceRoll } from "./field_data.slint";
import { Field, FieldAdapter } from "./field.slint";
import { LowerPanel } from "./lower_panel.slint";

import { InfoPanelAdapter, LowerPanelAdapter } from "./adapters.slint";

export { FieldAdapter, InfoPanelAdapter, LowerPanelAdapter }

export component AppWindow inherits Window {
	preferred-width: 1920px;
	preferred-height: 1080px;

	title: FieldAdapter.main_info_title;

	HorizontalLayout {
		VerticalLayout {
			Field {
				now_height: root.height * 0.9;
				now_width: root.width;
			}

			LowerPanel {
				panel_size: root.height * 0.1;
				preferred-width: 100%;
			}
		}
	}
}
